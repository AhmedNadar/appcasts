.casts
  - @casts.each do |cast|
    .cast.row
      .col-md-5
        = image_tag cast.image_url
      .col-md-7
        %span.num= "episode ##{cast.number} - "
        %span.app-title Software as a Service Application (fake)
        %h3= cast.title
        = cast.description
